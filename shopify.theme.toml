# Shopify Theme Configuration
# This file configures your Shopify theme development environment
# Note: Store selection is now handled during authentication

[development]
theme_id = ""  # Will be set automatically when you run shopify theme dev
ignore_files = [
  "config/settings_data.json"
]

[production]
theme_id = ""  # Set this to your live theme ID if you want to push directly
